{
  "name": "german-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "packageManager": "bun@1.3.14",
  "scripts": {
    "test": "bun test",
    "styles": "bunx @tailwindcss/cli -i ./src/styles.css -o ./src/tailwind.css --minify",
    "build": "bun run styles && bun build ./index.html --outdir ./dist --production",
    "build:api": "bun run build && bun ./scripts/copy-to-api.js"
  },
  "dependencies": {
    "react": "19.2.7",
    "react-dom": "19.2.7",
    "tailwindcss": "4.3.0",
    "@tailwindcss/cli": "4.3.0"
  }
}
