import React from "react";
import { Alert } from "../components/erp/Alert.jsx";

export function NotFoundPage() {
  return <Alert variant="error" title="Không tìm thấy màn hình">Đường dẫn này không tồn tại.</Alert>;
}
